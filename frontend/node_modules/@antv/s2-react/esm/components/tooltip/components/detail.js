import { TOOLTIP_PREFIX_CLS, } from '@antv/s2';
import React from 'react';
import { TooltipIcon } from './icon';
export const TooltipDetail = React.memo((props) => {
    const { list = [] } = props;
    return (React.createElement("div", { className: `${TOOLTIP_PREFIX_CLS}-detail-list` }, list === null || list === void 0 ? void 0 : list.map((listItem, idx) => {
        const { name, value, icon } = listItem;
        return (React.createElement("div", { key: `${value}-${idx}`, className: `${TOOLTIP_PREFIX_CLS}-detail-item` },
            React.createElement("span", { className: `${TOOLTIP_PREFIX_CLS}-detail-item-key` }, name),
            React.createElement("span", { className: `${TOOLTIP_PREFIX_CLS}-detail-item-val ${TOOLTIP_PREFIX_CLS}-highlight` },
                icon && (React.createElement(TooltipIcon, { icon: icon, width: 8, height: 7 })),
                value)));
    })));
});
TooltipDetail.displayName = 'TooltipDetail';
//# sourceMappingURL=detail.js.map
import { TOOLTIP_PREFIX_CLS, } from '@antv/s2';
import React from 'react';
import { ReactElement } from '../../../common/react-element';
import { TooltipIcon } from './icon';
export const TooltipInterpretation = React.memo((props) => {
    const { name, icon, text, content } = props;
    const renderName = () => name && (React.createElement("span", { className: `${TOOLTIP_PREFIX_CLS}-interpretation-name` }, name));
    const renderText = () => text && React.createElement("div", null, text);
    const renderElement = () => (React.createElement(ReactElement, { content: content }));
    return (React.createElement("div", { className: `${TOOLTIP_PREFIX_CLS}-interpretation` },
        React.createElement("div", { className: `${TOOLTIP_PREFIX_CLS}-interpretation-head` },
            React.createElement(TooltipIcon, { icon: icon, className: `${TOOLTIP_PREFIX_CLS}-interpretation-icon` }),
            renderName()),
        renderText(),
        renderElement()));
});
TooltipInterpretation.displayName = 'TooltipInterpretation';
//# sourceMappingURL=interpretation.js.map
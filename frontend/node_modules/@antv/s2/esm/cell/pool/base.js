export class BaseCellPool {
    constructor() {
        this.pool = [];
    }
    acquire() {
        return this.pool.pop();
    }
    release(cell) {
        this.pool.push(cell);
    }
}
//# sourceMappingURL=base.js.map
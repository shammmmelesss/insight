import { BaseCellPool } from './base';
export class DataCellPool extends BaseCellPool {
    release(cell) {
        if (!cell.getRenderer()) {
            this.pool.push(cell);
        }
        else {
            cell.destroy();
        }
    }
}
//# sourceMappingURL=data-cell.js.map
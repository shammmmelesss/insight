import type { PointLike } from '@antv/g-lite';
import { Node, CornerHeader as OriginCornerHeader, type BaseCornerOptions } from '@antv/s2';
export declare class CornerHeader extends OriginCornerHeader {
    static getCornerNodes(options: BaseCornerOptions & {
        position: PointLike;
        width: number;
        height: number;
    }): Node[];
}

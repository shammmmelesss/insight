import { ColCell } from '../col-cell';
import { BaseCellPool } from './base';
export declare class ColCellPool extends BaseCellPool<ColCell> {
    private readonly cellIdPool;
    acquire(): ColCell | undefined;
    release(cell: ColCell): void;
}

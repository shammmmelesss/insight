import type { PointLike } from '@antv/g-lite';
import { CornerBBox, Node, CornerHeader as OriginCornerHeader, PanelBBox, type BaseCornerOptions } from '@antv/s2';
export declare class AxisCornerHeader extends OriginCornerHeader {
    /**
     * Get corner Header by config
     */
    static getCornerHeader(options: BaseCornerOptions & {
        panelBBox: PanelBBox;
        cornerBBox: CornerBBox;
    }): AxisCornerHeader;
    static getCornerNodes(options: BaseCornerOptions & {
        position: PointLike;
        width: number;
        height: number;
    }): Node[];
    protected getCellInstance(node: Node): any;
}

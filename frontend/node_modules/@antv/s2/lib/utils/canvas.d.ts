/**
 * 获取工具 canvas
 * 需要把 canvas 插入到 body 下，继承全局的 css 样式（如 letter-spacing）
 * 否则后续的 measureText 与实际渲染会有较大差异
 */
export declare const getOffscreenCanvas: () => HTMLCanvasElement | null;
/**
 * 移除工具 canvas
 */
export declare const removeOffscreenCanvas: () => void;

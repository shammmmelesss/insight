import type { ViewMeta } from '../../common/interface/basic';
import type { Node } from '../../facet/layout/node';
export declare const isLinkFieldNode: (linkFields: string[] | ((meta: Node | ViewMeta) => boolean), meta: Node | ViewMeta) => boolean;

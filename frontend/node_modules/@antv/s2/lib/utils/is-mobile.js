"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isWindows = exports.isIPhoneX = exports.isMobile = void 0;
/*
 * 判断是否是移动端。
 * 兼容场景：pc端但是使用mobile配置。
 */
const common_1 = require("../common");
const ssr_1 = require("./ssr");
function isMobile(device) {
    if (device === common_1.DeviceType.MOBILE) {
        return true;
    }
    // SSR environment: treat as desktop
    if ((0, ssr_1.isSSR)() || !(0, ssr_1.hasNavigator)()) {
        return false;
    }
    return /(?:iPhone|iPad|SymbianOS|Windows Phone|iPod|iOS|Android|Mobile|Phone|Tablet)/i.test(navigator.userAgent);
}
exports.isMobile = isMobile;
function isIPhoneX() {
    // SSR environment: not iPhone X
    if ((0, ssr_1.isSSR)()) {
        return false;
    }
    // eslint-disable-next-line no-restricted-globals
    return (/iPhone/gi.test(navigator.userAgent) &&
        window.screen.height === 812 &&
        window.screen.width === 375);
}
exports.isIPhoneX = isIPhoneX;
function isWindows() {
    // SSR environment: not Windows
    if ((0, ssr_1.isSSR)()) {
        return false;
    }
    return /windows/i.test(navigator.userAgent);
}
exports.isWindows = isWindows;
//# sourceMappingURL=is-mobile.js.map